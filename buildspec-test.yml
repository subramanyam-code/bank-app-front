version: 0.2

env:
  variables:
    AWS_DEFAULT_REGION: ap-south-2
    ACCOUNT_ID: 455982474789
    IMAGE_REPO_NAME: bank-app
    IMAGE_TAG: latest

phases:
  pre_build:
    commands:
      - echo Logging in to Amazon ECR...
      - IMAGE_URI=$ACCOUNT_ID.dkr.ecr.$AWS_DEFAULT_REGION.amazonaws.com/$IMAGE_REPO_NAME:$IMAGE_TAG
      - aws ecr get-login-password --region $AWS_DEFAULT_REGION | docker login --username AWS --password-stdin $ACCOUNT_ID.dkr.ecr.$AWS_DEFAULT_REGION.amazonaws.com

  build:
    commands:
      - echo Pulling Docker image...
      - docker pull $IMAGE_URI

      - echo Running container...
      - docker run -d -p 8080:80 --name test-container $IMAGE_URI

      - echo Waiting for app to start...
      - sleep 20   # increased time

      - echo Checking running containers...
      - docker ps

      - echo Container logs...
      - docker logs test-container

      - echo Testing app (retry)...
      - for i in {1..5}; do curl -f http://localhost:8080 && break || sleep 5; done

      - echo Cleaning up...
      - docker stop test-container
      - docker rm test-container
