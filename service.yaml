apiVersion: v1
kind: Service
metadata:
  name: bank-app-service
spec:
  type: NodePort   # 🔥 change here
  selector:
    app: bank-app
  ports:
    - port: 80
      targetPort: 3000
      nodePort: 30007   # choose between 30000–32767